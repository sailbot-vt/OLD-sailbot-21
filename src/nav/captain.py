from collections import deque

from src.nav.tack_points import place_tacks


def waypoints_between(start, end, boat, wind):
    """Gets a queue of waypoints between the waypoints start and end.

    The result includes end but not start.

    Keyword arguments:
    start -- The first waypoint of the leg
    end -- The last waypoint of the leg

    Returns:
    A queue of waypoints"""
    return None
